//
//  Shape.swift
//  Fable
//
//  Created by Dmytro Ipatii on 17/06/2026.
//

import SwiftUI

enum Shape {

    static let pill: RoundedRectangle = RoundedRectangle(cornerRadius: Radius.pill)

    static let card: RoundedRectangle = RoundedRectangle(cornerRadius: Radius.card)

    static let button: RoundedRectangle = RoundedRectangle(cornerRadius: Radius.button)

    static let thumb: RoundedRectangle = RoundedRectangle(cornerRadius: Radius.thumb)

    static let avatar: Circle = Circle()

}
